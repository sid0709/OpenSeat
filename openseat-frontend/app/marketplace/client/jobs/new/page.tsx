"use client";

import { ClientDashboardView } from "@/src/client/components/ClientDashboardView";

/** Job creation demo. The form currently lives in the client workspace view. */
export default function NewClientJobPage() {
  return <ClientDashboardView />;
}
