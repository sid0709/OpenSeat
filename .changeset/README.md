# Release notes

Run `npm run changeset` for user-visible changes. Select affected packages and
patch (compatible fix), minor (compatible feature), or major (breaking change).
Describe behavior and migration steps. Tooling-only changes may use
`npm run changeset -- --empty`. See [release process](../docs/RELEASING.md).
